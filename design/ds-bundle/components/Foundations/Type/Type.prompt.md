Glance type. Exactly one Anton display element per screen: 160px hero percent, 88px hero word (READY/PAUSED/DONE/ERROR), or 58px live readout — it takes the phase color. Everything else is Space Grotesk: 30px row values (--text), 26px sublines (--text-soft) and verbs, 24px row labels (--dim), 20px filenames (--dim). Sizes are tuned for a 1024x600 touchscreen read at arm's length; don't shrink below 20px.
